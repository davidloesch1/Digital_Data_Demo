[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Railway’s Node default is `node index.js`; Dockerfile uses `collector.js` — set both paths explicitly.
startCommand = "node collector.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
